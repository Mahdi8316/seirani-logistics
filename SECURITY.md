# Security policy

اطلاعات ورود، فایل‌های `.env`، تنظیمات واقعی `php-site/inc/config.php`، دیتابیس‌ها،
بکاپ‌ها و کلیدهای خصوصی نباید در Issue یا Commit قرار گیرند.

اگر اطلاعات حساسی تصادفاً Commit شد، حذف فایل به‌تنهایی کافی نیست: ابتدا رمز یا
کلید را باطل و جایگزین کنید، سپس آن را از تمام تاریخچهٔ Git پاک کنید.

گزارش آسیب‌پذیری را عمومی منتشر نکنید؛ آن را به‌صورت خصوصی برای مالک مخزن ارسال کنید.
